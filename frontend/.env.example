# Public URL of the backend (no trailing slash).
# The "Continue with Microsoft" button points the browser at <VITE_BACKEND_URL>/auth/microsoft
# Local:      http://localhost:4000
# Production: https://sheet-automation-9br8.onrender.com
VITE_BACKEND_URL=http://localhost:4000

# Admin gate credentials shown on the login page (before the Microsoft button).
# These are baked into the built JS, so treat them as a light gate, not real auth.
VITE_ADMIN_USER=hng@admin
VITE_ADMIN_PASS=hng@4747
