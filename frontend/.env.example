# Public URL of the backend (no trailing slash).
# The "Continue with Google" button points the browser at <VITE_BACKEND_URL>/auth/google
# Local:      http://localhost:4000
# Production: https://api.yourdomain.com  (or https://yourdomain.com)
VITE_BACKEND_URL=http://localhost:4000
